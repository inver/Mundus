package bundled.shaders.defaultShader

import net.nevinsky.abyssus.core.Renderable
import net.nevinsky.abyssus.core.shader.DefaultShader
import net.nevinsky.abyssus.core.shader.ShaderConfig

class BundledDefaultShader extends DefaultShader {
    BundledDefaultShader(ShaderConfig config, Renderable renderable) {
        super(config, renderable)
    }
}